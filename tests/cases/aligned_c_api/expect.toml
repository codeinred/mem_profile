# posix_memalign + aligned_alloc attribution (2900 + 1536 bytes).
#
# On macOS these are hooked via the dyld interpose table; on Linux they are
# exported under their own names (glibc's versions do not route through the
# memalign hook and have no __libc_ aliases).

[totals]
typed_free_bytes = { min = 4436 }

[[types]]
type = "CAligned"
size = 16
objects = 1
bytes = 4436
