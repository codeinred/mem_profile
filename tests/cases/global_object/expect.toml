# Global destroyed at exit; the exit-time destructor chain is captured on
# both macOS and Linux. gh-1 (global tracking broken on Linux) was fixed by
# the mp_unwind final-frame fix: the dropped frame made the exit-time scan
# boundary unreachable.

[[types]]
type = "GlobalOwner"
size = 24
objects = 1
bytes = 3000
