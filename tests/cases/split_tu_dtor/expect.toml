# Dtor defined in a second TU. Regression guard for gh-2; same mechanism as
# out_of_line_dtor (external linkage forces eager CodeGen emission).

[ir]
instrumented = ["SplitTu"]

[[types]]
type = "SplitTu"
size = 24
objects = 1
bytes = 1500
